/**
 * daraja-js — type-safe Node.js/TypeScript SDK for Safaricom Daraja (M-Pesa).
 *
 * Public surface lands in Sprint 1 (auth + STK Push). This entry point exists
 * so the build, type, and test toolchain is wired from day one.
 *
 * @packageDocumentation
 */

/** Package version, replaced at build time. */
export const VERSION = '0.0.0';
